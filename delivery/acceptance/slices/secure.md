YOUR FILES: src/phctx/ingest.py, src/phctx/download.py, src/phctx/extract.py, src/phctx/model.py, src/phctx/backup.py, src/phctx/config.py, src/phctx/cli.py, src/phctx/__init__.py, evals/harness.py, evals/summarize.py, scripts/build_release.py, scripts/verify.py, a new scripts/run_manifest.py, pyproject.toml, ops/rollback.sh, tests/test_tools_files.py (download/extract tests only), and a new tests/test_security_ops.py.

FIX:
- F17 no plaintext TLS private key on disk: keep the ingest TLS private key PEM in the macOS Keychain (generic password, service 'phctx-ingest-tls', account 'phctx'; read with /usr/bin/security find-generic-password -w; write with add-generic-password -U -w via stdin). Python ssl cannot load a key from memory: load via a 0600 file in a private mkdtemp dir deleted right after load_cert_chain; document that residual. The public cert stays on disk. Keychain access must be injectable; tests use a fake (never the real Keychain).
- F18 ingest: BoundedSemaphore(16) around request handling (excess connections closed), absolute body-read deadline, handshake timeout.
- F19 download: one monotonic deadline covering DNS (resolver in a thread with timeout), connect, headers and body; tests with slow fake servers.
- F20 extract: remove preexec_fn; set rlimits inside the child entry (_child) before parsing; minimal env (PATH, PYTHONPATH, HOME only); keep the wall-clock timeout.
- F21/F22 model.py: never copy ~/.codex/auth.json. CODEX_HOME = private temp dir with config.toml and a SYMLINK auth.json -> the existing auth file (PHCTX_CODEX_AUTH override). Remove the temp home and the owned work dir in finally; keep the final message in memory only. Test with a fake auth path that no copy of its content is written.
- F28/F29: remove daily_budget_usd (not enforced); model_calls.cost_usd inserted as NULL (unknown), never 0; the openai_api backend raises ModelError('backend_not_implemented'); config.load rejects unsupported backends with a clear error.
- F42 backup: fsync each snapshot file and directory; manifest written temp+fsync+rename; fsync archive directory after rename.
- F43: add CLI 'phctx restore-premigration FILE.sqlite3 --new-root DIR' (backup.py) that builds a proper new root from a pre-migration SQLite file plus the live objects dir (only referenced objects, hash-verified) and verifies it opens; update ops/rollback.sh to use it. Test on a disposable root.
- F34/F35 harness: background positive cases require the gate actually queued (or shadow-queued) a candidate, not just a model 'surface'; negative cases record model-silence vs gate-blocked. Replace the process-global model.investigate monkeypatch with a per-case failing backend passed into worker.run_once (add an optional parameter there ONLY via the existing `scripted` argument — do not edit worker.py; use backend='scripted' with a raising callable).
- F37 summarize.py: exit 1 / status FAIL unless every critical case has the required runs (3), dev and holdout run_meta hashes match, and evals/score.py exits 0; do not rewrite prompt hashes.
- F38/F55: scripts/run_manifest.py writes delivery/run-manifests/NAME.json (src tree hash, contracts/prompts hashes, command, timestamp); build_release.py refuses a PASS whose evidence manifest hash differs from the current tree. Single version source: src/phctx/__init__.__version__ = '0.4.0'; pyproject uses hatch dynamic version from it.
- F40: scripts/verify.py verifies the delivered layout (src/phctx, tests), not reference_core.
