YOUR FILES: src/phctx/store.py, src/phctx/migrations.py, src/phctx/tools.py, src/phctx/worker.py, tests/test_store_v2.py, tests/test_worker_backup.py, and a new tests/test_invariants.py.

FIX:
- F01 catalog/latest-sample staleness on same-key endpoint shrink (store.ingest_batch + _apply_catalog): read the previous row's start_at/end_at; if an update removes a boundary (old start == catalog first_at, old end == catalog last_at, or old end == source latest_sample_at) recompute that (source, metric, unit) group and the source latest via indexed queries. Add a randomized reference-oracle test (upserts incl. endpoint shrink, metric/unit changes, deletes, re-inserts) asserting observation_catalog == GROUP BY ground truth and sources.latest_sample_at == max(end_at) of active rows after every batch.
- F02/F03 canonical supersession independent of arrival order, preserving tombstone authority: migration v5 creates table supersessions(origin_key TEXT PRIMARY KEY, live_observation_id TEXT NOT NULL); backfill from every live row (source_id LIKE 'apple_health:%', deleted or not) having origin_key; canonical_observations := active rows that are NOT (source_id='apple_health_export' AND origin_key IN supersessions). Stop using superseded_by_live (leave the column). In ingest_batch, for live sources INSERT OR IGNORE supersessions for every upserted sample's origin_key; tombstones never delete supersessions. Test: permute live-first / export-first / live-then-deleted and a v4->v5 migration; canonical sets must be identical.
- F13: take the pre-migration snapshot whenever the DB has any records OR observations.
- F11 evidence binding: queue_insight compares the candidate's evidence_versions (ref -> version), when non-empty, against current versions inside the transaction; mismatch -> StoreError('stale_evidence'). Add Store.read_versions(refs) -> dict. context_read and context_read_original results include a 'versions' map for the refs returned so a model can echo what it read. Test: read V1, change the observation, queue candidate with V1 -> stale_evidence.
- F14 post-commit truthfulness (tools.context_capture_file): once put_attachment_bytes returns a receipt, no later exception may turn the result into an error; extraction/metadata failures become extraction_status='failed' plus extraction_error. Test: monkeypatch extract.extract and object_info to raise sqlite3.OperationalError after commit; the result must be the committed receipt.
- F24 shadow separation: migration v5 adds table shadow_insights; shadow candidates go only there (Store.queue_shadow), never into insights, never returned by pending_insights/bootstrap, never counted in attention budget or real-insight dedup. Worker calls queue_shadow when worker_mode == 'shadow'. Fix the shadow test in tests/test_worker_backup.py to assert hidden behaviour (F53).
- F25 semantic interval: no new revisit job for a question while one is queued/running for it — merge new evidence into that job's payload instead; the minimum interval is measured from the last EXECUTED (state done) job for that question. Test: changes every 15 minutes (manipulated timestamps) yield one job, not a train.
- F27 lease fencing: job completion updates and result queueing verify lease_owner is still this worker (UPDATE ... WHERE id=? AND lease_owner=?; zero rows -> drop the result). Test: expire lease, another owner claims, the old owner's completion is ignored.
- F26 partial: deletion-only observation changes carry the metrics they removed in the change detail.
