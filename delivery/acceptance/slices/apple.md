YOUR FILES: src/phctx/apple_export.py, tests/test_apple_ingest.py, and a new tests/test_apple_mapping.py.

FIX (per contracts/normalization.md):
- F04 export native_id = 'x2:' + sha256 over canonical JSON of ALL element attributes and all children (metadata, statistics, events, file refs, beat points, HRV metadata lists), first 40 hex — no rounding, nothing dropped. origin_key stays the separate equivalence key computed per the contract (unit normalization, category string values, workouts [activity_type, round(seconds)]).
- Bump PARSER_VERSION to 4. Old production rows use 'x:' native ids. After a successful full import under a new parser version, retire older export rows of the SAME export so the archive is not doubled: every row stores metadata key 'import': {'parser': N, 'export': sha16}; rows lacking it count as older. Tombstone older rows for that export in pages via store.ingest_batch(deleted_ids=...) so receipts and catalog stay correct. Test with a synthetic archive imported with v3-style ids and then v4.
- F07 preservation: keep every InstantaneousBeatsPerMinute (bpm, time) and HeartRateVariabilityMetadataList point, all WorkoutEvents (no 200 cap), all WorkoutStatistics attributes, and totals with their units, in the sample metadata. Count unmapped elements in 'unsupported_elements'.
- F06 ActivitySummary: keep local_date in metadata and the instant window anchored in the user's timezone [local midnight, next local midnight); test spring-forward and fall-back dates and that calendar identity never shifts.
- F08: never exceed 5000 samples per ingest_batch (multi-row summaries included); normalize `since` to an instant before comparing; include import options (since, parser version) in page request ids.
- F09: persist a GPX only when referenced by a permitted parent workout (FileReference path); ECG CSVs only if their header lines (Device/Software/etc.) do not match restricted_origin. A route referenced only by a restricted workout must not be stored; test it.
- F10: stream ZIP members with a bounded read (refuse members above the cap before reading all bytes). Route attachment occurred_at = parent workout start; ECG occurred_at = 'Recorded Date' header if present, else payload event_time_unknown=true and the export date.
- F47: stop asserting CDA is a duplicate. Stream export_cda.xml, sample up to 2000 observations (heart rate / respiratory rate / oxygen saturation), check each has an export.xml Record with the same type/time/value, and record matched/unmatched counts in import_runs counts ('cda': {...}). Also record per-element-kind counts.
